name: GitHub Repository Replica

on:
  workflow_dispatch:
    inputs:
      source_repo_url:
        description: "Source Repository URL"
        required: true
        type: string

      target_repo_name:
        description: "Target Repository Name"
        required: true
        type: string

      visibility:
        description: "Repository Visibility"
        required: true
        default: private
        type: choice
        options:
          - private
          - public

jobs:
  clone-repository:
    runs-on: ubuntu-latest

    env:
      GH_TOKEN: ${{ secrets.GH_PAT }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify GitHub CLI
        run: |
          gh --version

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Authenticate GitHub
        run: |
          gh auth status

      - name: Get Authenticated User
        id: github_user
        run: |
          USERNAME=$(gh api user --jq '.login')
          echo "username=$USERNAME" >> $GITHUB_OUTPUT
          echo "Authenticated User: $USERNAME"

      - name: Create Target Repository
        run: |
          OWNER="${{ steps.github_user.outputs.username }}"
          REPO="${{ github.event.inputs.target_repo_name }}"
          VISIBILITY="${{ github.event.inputs.visibility }}"

          if gh repo view "$OWNER/$REPO" >/dev/null 2>&1; then
            echo "✅ Repository already exists"
          else
            gh repo create "$OWNER/$REPO" --$VISIBILITY
            echo "✅ Repository created as $VISIBILITY"
          fi

      - name: Mirror Clone Source Repository
        run: |
          echo "📥 Cloning source repository..."

          git clone --mirror \
            "${{ github.event.inputs.source_repo_url }}" \
            repo.git

      - name: Push Repository Mirror
        run: |
          cd repo.git

          TARGET_URL="https://x-access-token:${GH_TOKEN}@github.com/${{ steps.github_user.outputs.username }}/${{ github.event.inputs.target_repo_name }}.git"

          echo "🚀 Pushing all branches, tags and refs..."

          git remote set-url origin "$TARGET_URL"

          git push --mirror

      - name: Verify Repository
        run: |
          echo ""
          echo "====================================="
          echo "Repository Clone Completed"
          echo "====================================="
          echo ""
          echo "Source Repository:"
          echo "${{ github.event.inputs.source_repo_url }}"
          echo ""
          echo "Target Repository:"
          echo "https://github.com/${{ steps.github_user.outputs.username }}/${{ github.event.inputs.target_repo_name }}"
          echo ""
          echo "Visibility:"
          echo "${{ github.event.inputs.visibility }}"
          echo ""

      - name: Display Repository Details
        run: |
          gh repo view \
            "${{ steps.github_user.outputs.username }}/${{ github.event.inputs.target_repo_name }}" \
            --json url,name,visibility

      - name: List Branches
        run: |
          echo "📋 Branches copied:"
          
          gh api \
            repos/${{ steps.github_user.outputs.username }}/${{ github.event.inputs.target_repo_name }}/branches \
            --jq '.[].name'

      - name: Success
        run: |
          echo "✅ Repository replication completed successfully."
          echo "✅ All branches copied."
          echo "✅ All tags copied."
          echo "✅ All repository contents copied."
          echo "✅ Visibility: ${{ github.event.inputs.visibility }}"
